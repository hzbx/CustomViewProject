<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hzb.customviewproject">

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.PullDownRefreshActivity" />
        <activity android:name=".ui.XfermodeActivity" />
        <activity android:name=".ui.MuchShadeActivity" />
        <activity android:name=".ui.GestureActivity" />
        <activity android:name=".ui.TestActivity" />
        <activity android:name=".ui.SystemBarActivity"></activity>
    </application>

</manifest>